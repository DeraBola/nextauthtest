import React from 'react'

function LoginForm() {
  return (
    <form>
      <button>
        Sign In with Google
      </button>
         <button>
        Sign In with Google
      </button>
    </form>
  )
}

export default LoginForm
